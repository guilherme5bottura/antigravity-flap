rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leitura e gravação no ranking de pontuações
    match /leaderboard/{scoreId} {
      allow read, write: if true;
    }
  }
}
